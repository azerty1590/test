{
  "name": "hide-seek-paint-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension: paint a slab to hide on any web page, then seek it.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "icons": "node scripts/make-icons.js",
    "smoke": "node scripts/smoke.js",
    "pack": "node scripts/build-demo.js && rm -f dist/hide-seek-paint.zip && zip -qr dist/hide-seek-paint.zip manifest.json background.js content popup icons -x \"*.DS_Store\" && ls -la dist",
    "demo": "node scripts/build-demo.js"
  }
}
